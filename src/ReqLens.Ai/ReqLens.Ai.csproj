<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- The Document/JSON bridge and the chain internals are implementation detail, but they are
         where the subtle bugs live, so the test project sees them. -->
    <InternalsVisibleTo Include="ReqLens.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ReqLens.Domain\ReqLens.Domain.csproj" />
    <ProjectReference Include="..\ReqLens.Validation\ReqLens.Validation.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="AWSSDK.BedrockRuntime" Version="4.0.101.4" />
    <PackageReference Include="AWSSDK.Textract" Version="4.0.100.11" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Options" Version="10.0.11" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
